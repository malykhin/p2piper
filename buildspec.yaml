version: 0.2

phases:
  install:
    commands:
      - node -v
      - npm ci

  pre_build:
    commands:
      - mkdir output

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'

    